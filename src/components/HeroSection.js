import React from 'react';
import storyImg from '../assets/background.jpg';

export default function HeroSection() {
  return (
    
<section className="hero">
  <img src={storyImg} alt="Delicious Pasta" className="hero-img" />
</section>

  );
}
